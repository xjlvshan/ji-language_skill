---
name: ji-language
description: 使用「极语言」（D:\SEC 的中文编译型编程语言，Sc.exe 编译器，源码 GBK+CRLF）编写、编译、运行与调试程序。含已验证语法速查、编译运行工具脚本、致命陷阱清单、可运行示例，以及调用外部 DLL / ONNX Runtime 推理 / GGUF 量化模型的实测方案。
whenToUse: 当任务涉及读写 .sec/.SEC 源码、在 D:\SEC 下运行 Sc.exe/Sec.exe、编写极语言控制台或 GUI 程序、排错编译错误（未定义的名称/语法有误/内部错误）、或要用极语言调用外部 DLL 做 AI 推理（onnxruntime.dll / llama.dll / GGUF 量化模型）时使用。
---

# 极语言（SEC / Sc.exe）开发技能

极语言是 D:\SEC 下的一套**中文关键字编译型语言**：源码是纯文本，编译器 `Sc.exe` 直接产出 PE（控制台或 GUI）。
本技能记录的是**实测结论**（每条都标注了验证方式），不是文档抄录——该编译器有不少反直觉的坑，按文档照抄很可能编译失败或程序死循环。

## 一、30 秒上手

```powershell
# 1) 写源码：必须是 GBK 编码 + CRLF 换行，扩展名 .txt（.sec 是 IDE 的二进制格式，不要用）
$gbk=[Text.Encoding]::GetEncoding('GB18030')
$src=@'
程序类型=2
模块文件=CMD.inc
程序 初始启动
写文字("你好，极语言！");
写字符(13);写字符(10);
整数 a=7,b=2;
写格式("a+b=%d a/b=%d\r\n",a+b,a/b);
整数 sq=平方(a);
写格式("平方=%d\r\n",sq);
结束
程序 平方(数)
返回(数*数)
结束
'@
[IO.File]::WriteAllText('D:\SEC\deepseek-sec\examples\demo.txt',$src,$gbk)

# 2) 编译 + 运行（脚本会自动纠正编码/换行，并重试偶发假死）
powershell -File D:\SEC\deepseek-sec\tools\sec.ps1 -Src D:\SEC\deepseek-sec\examples\demo.txt -Run
```

输出形如：`BUILD_OK` → 编译器消息 → `ARTIFACT: ...\demo.com` → `--- program output ---` → 程序 stdout。

> **三条救命规则**（踩坑时先想它们）：
> 1. 源码必须 **GBK + CRLF**（`sec.ps1` 会自动纠正，命令行手工编译时要注意）。
> 2. 入口 = 文件里**第一个** `程序`，辅助例程必须写在它后面。
> 3. 出现**卡死/崩溃/0xC00000xx**：先试在配置区加一行 **`定义 位数=64;`**——它救活局部 `文本` 带初值、结构体含文本成员、子类无参方法这几类卡死；还不行再用 `sec.ps1` 重跑一次（编译后首次运行偶发假死）。

GUI 程序编译用同样命令（去掉 `-Run`），窗口截图用：

```powershell
Start-Process D:\SEC\deepseek-sec\examples\gui-1.exe; Start-Sleep 3
powershell -File D:\SEC\deepseek-sec\tools\shot.ps1 -Title <窗口标题子串> -Out D:\SEC\deepseek-sec\shots\gui-1.png
Stop-Process -Name gui-1 -Force
```

## 二、源码结构（顺序敏感）

```
程序类型=2          ← 第 1 行，必须最先：0=Windows/GUI(默认) 1=扩展DLL 2=控制台 3=函数类库
模块文件=CMD.inc    ← 第 2 行，控制台用 CMD.inc；GUI 用 GUI.inc
定义 位数=64;       ← 可选，可多行（必须在上面两行之后）。★ 出现「局部 文本 带初值 / 结构体含文本成员 /
                      子类方法」时把 位数 写成 64 能救活这些卡死（实测：32 位卡死 → 64 位正常输出，产物 machine=0x8664）
定义 后缀=exe;      ← 可选，改产物扩展名（默认控制台是 .com，GUI 是 .exe）

程序 初始启动        ← 入口：文件里第一个 程序/函数 就是入口，辅助函数必须写在它后面！
...
结束
程序 平方(数)        ← 辅助函数必须放入口之后
返回(数*数)
结束
```

- 语句用**换行或分号**分隔；关键字是中文；`否则`/`为` 等后面必须有空格。
- 注释：`//`
- 全局变量写在第一个 `程序` 之前；函数内声明的（含参数）都是局部变量，可加 `局部` 前缀强调。
- `.com` 产物**就是可直接运行的 PE**：默认 32 位（machine=`0x14C`），加 `定义 位数=64;` 则为 64 位（machine=`0x8664`）。

## 三、致命陷阱（实测，务必避开）

| 陷阱 | 现象 | 正确写法 |
|---|---|---|
| `文本 s[64]; s="字面量";`（**运行期**赋值）或**例程内**声明带初值 `文本 s[32]="abc";` | **死循环卡死**，连前面的输出都可能看不到 | ①**全局声明行给初值是可以的**（`文本 全局文本[32]="文本";` ✅ 实测）；②运行期用 `内存复制(s,"文字",字节数)` / `s=s+"文字"` / `整数 p="文字";`；③★ 加 `定义 位数=64;` 后连局部带初值也正常（实测 w64a → `abc`） |
| 结构体含 `文本` 成员、子类方法（无参无返回）卡死/崩溃 | 0xC000001D 崩溃或 TIMEOUT | ★ 加 `定义 位数=64;`（实测 w64b → `生命=100 名字=赵子龙`、w64c → `说话被调用`） |
| 子类方法带参数或带返回值 | 即使 64 位也 TIMEOUT | 本版只跑通「成员 + 无参无返回值方法」；参数用全局变量/对象成员传递 |
| 数据表放 `文本` 字段 | 崩溃(0xC0000005)或卡死 | 数据表只放整数类字段（含字节）；文本另存平行数组 |
| 单个结构变量大括号初始化 `变量={10,20,800,600}` | 0xC0000005 崩溃 | 用结构数组形式赋值，或逐个 `变量.字段=…` |
| `定义 名称=数值;`（非配置项） | `内部错误 -> Subscript out of range` | 普通常量用 `常量 名称=值;`；`定义` 只用于位数/后缀/宏指令（`定义 存入="50:0";`） |
| `长()` / `长度()` | `符号 长 不存在` | 用 `.长度`（仅对 `[n]`/`={…}` 声明有效）、`文字长度()`、`文件大小()` |
| `置入[小数 3.14]` | `置入错误:小数` | `置入[字节/整数/整形 …]` 可用 |
| 动态调用 `加载库`/`函数地址`/`调用 变量()` | 4/4 TIMEOUT 零输出 | 用 `引入 "lib\kernel32.lib";` + `导入 中文名 [别名 原名] 支持库 "XX.DLL",参数个数`（个数必须准；无参 API 直接写名字不带括号） |
| ★**修正**：「`调用 变量()` 动态调用不可用」的旧结论 | 只要函数指针存在 `整数` 里就必然失败 | **实测可用**：指针必须存 `大数`/`变数`（`整数` 在 64 位下也是 32 位，会把 `0x7FFAA7990000` 截成 `0xA7990000`，接着 `GetProcAddress` 恒返回 0、调用空指针→0xC0000005）。`大数 f=取址(库,"名"); 大数 r=调用 f(参数...);` 0~8 个参数实测全部正确。详见 `references/11-ai-runtimes-onnx-gguf.md` |
| `调用 导入的中文API名(...)`（把 `调用` 当直接调用用） | `符号 X 不存在` | `调用` 是「间接调用」关键字，只能跟保存函数地址的变量；直接调用写 `名称(参数);` |
| 64 位产物里写 `指针(偏移)`（无后缀，读 1 字节） | 必崩 `0xC0000005`（32 位产物正常） | 加后缀 `(偏移)$`8字节 `&`4字节 `%`2字节 `!`单精度 `#`双精度；逐字节解析二进制格式就**不要写** `定义 位数=64;` |
| `如果`→`循环于`→`如果` 三层嵌套，或 `循环于` 里再嵌 `循环于` | 编译通过但逻辑悄悄错（实测「跳过 2 个元素」变成跳过 0 个） | 把内层逻辑拆成独立 `程序` 再调用 |
| `模块文件=CMD.inc;`（配置行多写分号/空格） | `语法有误 + 内部错误` | 配置行一律不加多余符号 |
| 长字符串 `内存复制` 进定长 `文本` 缓冲 | 越界冲坏相邻全局变量，后面逻辑全乱/崩 | 先按缓冲大小截断长度，**再**拷贝 |
| 程序崩溃时 stdout 全丢，查不到停在哪 | `-Run` 只见异常退出码、零输出 | 关键步骤后加 `冲刷(0)`（`导入 冲刷 别名 fflush 支持库 "MSVCRT.DLL",1;`） |
| `判断(参数 & $FFFF)` 之类**在表达式/条件里对变量用符号位运算** | `语法有误 + 内部错误` | 先赋给变量再判断：`整数 编号=参数 & 65535;` 然后 `判断(编号)` |
| 字符串里写 `\"` 想嵌引号 | `未定义的名称:…` | 极语言字符串**不支持 `\"` 转义**；用 `''` 表示引号（实测 `''`→`"`）或分开拼接 |
| `设置定时器` | 语法有误 | 用 `设置定时(窗口,编号,毫秒,0)`，`毫秒=0` 删除。★**第 4 参数千万别传 `@过程`**：TIMERPROC 蹦床与 USER32 的调用约定/清栈不匹配，回调几次后栈被搞坏 → 必 `0xC0000409`（实测 500ms 定时器 + 只加计数的回调，12s 内必崩，证据 `D:\SEC\project\ji-offline-translator\src\_lab\tm5.txt`）；传 `0` 让系统投递标准 `WM_TIMER`、在 `窗体消息` 里写 `为 定时事件{...}` 则稳定（tm4，12s 计数 29）。传了非 0 回调时系统**不再投递 WM_TIMER**，两种写法不会同时生效 |
| 把**消息常量**当函数调用（如 `显示窗口(窗体,0)`） | `语法有误 + 内部错误`，且**报错行号比真正出错行小 1~2 行**，极易误判成别处代码的错 | `显示窗口` 是常量 `WM_SHOWWINDOW($0018)`；ShowWindow 的中文名是 **`显隐窗口`**。名字一律以 `D:\SEC\inc\lib\*.lib` 的 `英文名,参数个数,中文名` 表为准 |
| 鼠标消息里 `参数` 取坐标恒为 0（框选/拖拽全错） | 消息原型是 `程序 窗体消息(窗口,消息,参数,数据)`，**`参数`=wParam、`数据`=lParam** | 鼠标坐标在 **`数据`** 里（低 16 位 x、高 16 位 y，按 16 位有符号还原）；`单击左键`($0111) 的通知码/控件 ID 才在 `参数` 里 |
| `如果(a){如果(b){c};返回(0)}` | `语法有误 + 内部错误` | `如果` 体里嵌了 `如果` 之后不要再跟别的语句；把内层判断拆成独立 `程序` |
| 过程名与全局变量同名（`整数 刷窗体;` + `程序 刷窗体(...)`） | 整段代码以奇怪行号报 `语法有误` | 给过程改名（如 `背景刷窗体`） |
| 全局 `文本 大缓冲[10000000];` | exe 体积跟着涨（1 MB 数组 → exe +1 MB） | 全局文本数组进 PE 数据段；大数据用运行时 `申请内存`，或像逐行抓屏那样小块缓冲 + 循环 |
| `画圆角矩形` | 语法有误 | 函数名是 `圆角矩形`（`画方块/画椭圆/画线/写字到` 才带「画」） |
| GUI 里想给 Edit 改文字，看到"改不动"的旧说法 | — | **实测有效**：`改控件字(父窗体,控件ID,文本)` 对 Edit、Static、Button 都生效（见 `12-gui-5` 截图）；改不动时先核对控件 ID 与父窗体 |
| `缓冲=""+"abc"` | 输出 `0abc`（空字面量被当整数 0） | 别用空字面量做左操作数 |
| 文本缓冲区没清零就拼接 | 粘上别的缓冲区残渣 | 拼接前 `缓冲(0)=0;` |
| `写格式("%s", 文本变量)` | **实测可用**（正确填充的缓冲区 → `s=abc`）；早期"卡死"观察实为踩了 `文本=字面量` 的坑 | 正常用；但 `写格式("%s", 结构体.文本字段)`（如 `项.名称`）会 **0xC0000409 崩溃** → 结构体文本字段一律 `写文字(项.名称)` |
| `-Run` 只见 TIMEOUT / 零输出，程序其实是正常跑完的 | 被超时杀掉时 CRT 缓冲未刷新，输出丢失 | 让程序正常结束；必要时结尾加 `退出(0);`（实测 exit=0 且输出完整） |
| `信息框(0,"内容","标题",样式)` 报语法有误 | 它不是内置符号 | 先 `导入 信息框 别名 MessageBoxA 支持库 "USER32.DLL",4;`；控制台里弹框**必阻塞**（线程+WM_CLOSE 也救不回）→ 自动化改用 `延时框`（超时自动关，实测返回 32000） |
| `查找文件类 文件` / 文件大小 | 枚举异常或取不到大小 | 声明要带长度：`查找文件类 文件(长度=318)`；大小读 **`.长度`**（nFileSizeLow，`.大小` 是高 32 位）；同一目录 60+ 文件全量枚举可能 0xC0000409 → 用窄通配（如 `"zz*.txt"`）分片 |
| 注册表 `查询值项`/`改子项值` | 参数数量或 0xC0000005 崩溃 | 都是 **6 参数**（最后一个是 `@长度`）；数据参数必须传**文本变量或 `申请内存` 指针**（传字面量/全局整数地址会崩）；**注册表与内存/磁盘操作混在同一程序会崩** → 单独写一个程序；用前 `引入 "lib\advapi32.lib";` |
| `引入 "..\inc\哈希加密.sc";` | `内部错误 -> Type mismatch` | `.sc` 模块源码**不能引入**，只能把其中代码**内联**进源码（`examples/14-sys-7.txt` 内联了 MD5/SHA1，摘要与标准值逐字节一致） |
| 辅助例程写在入口**前面** | 它就成了入口：无输出 / 参数变栈地址 / 退出码=它的返回值 | 入口（文件里第一个 `程序`）必须最先出现 |
| `判断(...)` 链自己写 `结束` | 提前结束整个程序，后面局部变量报「未定义的名称」 | 判断链不写 `结束`，让外层 `结束` 收尾 |
| `%` 取模 | `a%b` 编译器内部错误；`a%=2` 卡死 | 中文：`整数 m=a 余 b;` |
| **变量**做符号位运算 `数|值` | `语法有误` | 变量必须用中文 `数 或 值` / `与` / `异` / `反 数` / `非 数` / `数 右移 值`；符号形式（`1|2`）只对字面量有效 |
| 把比较当表达式：`整数 甲=数>值;`、`写格式("%d",数>值)` | 语法有误 | 比较只能放 `如果/判断/循环/如果是` 条件里；取值用 `如果是` |
| `&&` `\|\|` `而且` `或者`、中文比较词 `数 大于 值` | 未定义/语法有误 | 多条件用嵌套 `如果`；比较用 `> < = <> >= <=` |
| `写格式("%f", 小数/浮点变量)` | 打印 `0.000000` | **根因：`小数/浮点` 是 4 字节 float，而 printf 的 `%f` 要 8 字节 double**。写法：拼进文本 `b=b+""+f`、或 `格式化(b,"%.4f",d)`、或先用 `双精` 接收再 `%f`、或 `3.5#` 双精字面量 |
| `检索(数组,值)` 只给两参 | `未知的数组类型：数列` | 必须三参：`检索(数列,9,8)` |
| `重置/保留/销毁/循环数组` 用在普通数组 | `无法重置:X` / 卡死 / 语法错误 | 本版这四个对普通数组不可用；`清零`、`内存复制`、`文字长度` 可用 |
| 方括号+初始化列表 `整数 甲[8]={…}` | 报「未知的数组类型」或数据是垃圾 | 用 `={…}` 自动长度，或 `[8]` 逐个赋值 |
| 二维数组 `表(i)(j)` | 读写数值错乱 | 本版别用二维数组存数据 |
| `大数`（64 位） | 按 32 位回绕（`2000000*5000`→`1410065408`），字面量超 32 位直接 Overflow | 别用 |
| 形参默认值 | 省略实参得到垃圾值（默认值不生效） | 调用写全实参 |
| `返回(:表达式)` 返回浮点 | 得到 0.000 | 用双精变量/文本输出 |
| 0 参例程写成 `名称();` | `参数数量不符,应该有0个参数` | 写 `名称;` |
| `跳转>标记` | 依赖上一条比较的标志位，时跳时不跳 | 用 `跳到 标记` |
| 退出码 | `结束` 的退出码 = 最后一条语句的返回值（`写格式` 是打印字符数） | **不要用退出码判断成败** |
| 变量名与内置函数重名（`写句`、`全局计数`） | 编译提示 `已存在函数名:X`（仍能编译） | 换个名字 |
| LF 换行的源码 | 「内部错误 / Invalid procedure call or argument [:第0行]」 | 必须 CRLF（`sec.ps1` 会自动修正） |
| UTF-8 源码 | `未定义的名称:绋嬪簭绫诲瀷`（乱码关键字） | 必须 GBK（`sec.ps1` 会自动修正） |
| 报错行号 | 行号 = 源码行号 − **配置行数**（配置行不计入） | 定位时把行号加回配置行数 |
| `函数` 定义辅助例程 | 某些上下文报 `语法有误 + 内部错误`（也有用 `函数`+`{}` 成功的例子） | 优先用 `程序`；要用 `函数` 就单独验证 |
| 首次运行刚编译的程序偶发假死 | 超时不出结果 | 重试即可（`sec.ps1 -Run` 已自动重试一次） |
| `程序类型=`/`模块文件=` 写在同一行或次序颠倒 | `未定义的名称:模块文件` | 一行一对，且顺序固定（类型→模块） |
| 想调用 Winsock（`网络启动/分配/连网/发送/接收`）却报 `语法有误` | 默认只链 kernel32/msvcrt | ★ **模块列表可以逗号分隔**：`模块文件=CMD.inc,dll.inc`（`dll.inc` 引入 `ws2_32.lib`）→ 网络函数立即可用（实测 `网络启动=0`、`连网 127.0.0.1:3080=0`、关闭端口 `=-1`） |
| `地址类 IP(协议=2)` | `未定义的名称:地址类` | 用 16 字节缓冲区手工拼 sockaddr_in：`文本 地址[16]; 地址(0)%=2; 地址(2)%=反转(端口); 地址(4)&=转换地址("127.0.0.1");` |
| 在 `如果/判断` 体里写 `结束` 想"提前退出" | **整个程序在那里就结束了**，即使条件为假也会丢掉后面的代码（实测 p2：只打印 `before`，`after` 永不执行） | 不要把 `结束` 当 return 用；用 `如果/否则` 分支结构，或 `标记`+`跳到` |
| 结构体数组 `玩家 队伍[3];` / `玩家 队伍(3);` | 编译通过但运行即卡死（控制台全空白） | 改用 `数据表 记录{...}` + `重置 记录(3)`，或并行数组；单个结构体变量正常可用 |
| 在单文件里写子类方法（`子类` 内的 `程序 …`）并调用 | 卡死、无输出 | 子类**成员变量**可正常读写；方法需按文档放到独立模块文件（先用全局函数操作对象） |
| ★**`BUILD_OK` 但 `CreateProcess` 报 193（不是有效 Win32 程序）** | 编译"成功"、PE 头自洽、文件长度也对，但 Windows 拒绝加载 | 特定源码形态会产出坏镜像（实测：过程里做 JSON 扫描 + 嵌套循环）。删掉/改写该过程即恢复。**用 `scripts/build-check.ps1` 编译后校验产物能否加载**，别只看 `BUILD_OK` |
| ★**`0xC0000005`：在 `如果(...)` 条件里调用自定义过程** | `如果(取键文字(显示,"result",临时值,4096)>0){…}` 必崩 | 先赋值再判断：`整数 长=取键文字(…); 如果(长>0){…}` |
| ★**`0xC0000005`：在自定义过程里对「参数」做指针加减** | `内存复制(基底+n,尾部,len)`（基底/尾部都是参数）必崩；`发送(网,参数缓冲,长,0)` 也不可靠 | 改成**全局缓冲 + 偏移**；拼串用 `格式化(基底,"%s%s",前缀,尾部)` |
| ★**`0xC0000409`（栈缓冲溢出）** | 在**定时器回调**里连续走「网络请求 → 写库 → 刷新列表」多层调用，数秒后崩 | 把这段从定时器回调挪到**按钮事件**即完全稳定 |
| 轮询等待循环不提前退出（GUI 程序） | `循环(n<上限){延时;n++;读文件;如果(端口>0){n=9999}}` 在 GUI 里跑满全部次数（控制台同写法正常） | 别长时间阻塞消息循环；改「用到时再读」的惰性方式 |
| 过程里**局部声明写在语句之后** | 报「未定义的名称:xxx」或运行期崩 | 每个 `程序` 开头先把**所有局部声明**列完 |
| `循环(1){…}` 只跑一次 | 死循环写成 `循环(1)` 就只执行一遍 | `循环(n)` = 重复 n 次；死循环写 `循环` 或 `循环{…跳出}` |
| `文件写入(字面量,1,n,句柄)` | 必崩 | 数据必须来自 `文本` 变量/缓冲，先 `放置(缓冲,"文字")` |
| `改控件字(窗体,ID,文本)` 对某些 Edit 崩 | 对多行只读 Edit 实测会崩 | 控件内容读写改用官方 `~` 运算符：`文本变量 ~ 控件`（读）/ `控件 ~ 内容`（写），见 `references/12-gui-app-practice.md` |
| 手写「整数取十进制数位」循环结果错乱 | `余=值-(值/10)*10;缓冲(i)=48+余;` 某过程里首轮算出 `余=50`（应为 0），字节变成 `b` | 整除本身是对的（实测 `50/10=5`、`7/2=3`）；别手写数位循环 → `格式化(缓冲,"%08d",值)` |
| 外部程序用 `SetWindowTextA` 灌入的文字，本进程内 `~` 读不到 | 外部 `GetWindowTextA` 看得到、程序内读到空串（自动化测试假象，极易误判） | 自动化请用 `PostMessageA(编辑框,0x0102,字符,0)` 逐字符模拟真实键入 |

## 四、语法速查（全部实测通过）

十一份参考手册（每条结论都带实测证据或明确标注"文档未实测"）：
- `references/01-toolchain.md` —— 编译器/IDE 行为、产物规则、`.sec` 二进制格式逆向、排错速查
- `references/02-language-core.md` —— 词法、类型、运算符、控制流、函数、数组/文本/指针（含可运行样例与真实输出）
- `references/03-basics.md` —— 完整类型表、运算符表、关键字总表（标注本版支持情况）、文档行号索引
- `references/04-advanced.md` —— 指针/内存、结构体、数据表、子类对象、常量、DLL 导入与被调用
- `references/05-console-system.md` —— 控制台 API/颜色/标题、命令行参数、文件读写、**网络 TCP（实测跑通：模块列表 `CMD.inc,dll.inc` + 16 字节 sockaddr）**、进程/注册表线索
- `references/06-gui.md` —— GUI.inc 骨架、创建窗口参数表、控件/消息/事件、截图验证工作流
- `references/07-basics-detailed.md` —— 基础语法**逐条实测详解**（38KB：环境硬约束、入口规则、类型全表、转义、运算符、分支循环跳转、文本数组、浮点转文本专题、**28 条陷阱总表**、示例清单、疑点）
- `references/08-advanced-detailed.md` —— 进阶**逐条实测详解**（32KB：指针/内存/检索、结构体、数据表、常量与中括号、跳到/跳转、机器指令 `指令[]`/`置入`、DLL 最小模板与 MessageBoxA 截图、子类、列举、预期失败反例、疑点）
- `references/09-gui-detailed.md` —— GUI **逐条实测详解**（42KB：GUI.inc 骨架、创建窗口 12 参、风格常量与控件类名、事件模型、取改控件、控件消息常量、字体与 GDI、4 个示例截图、两个 aux、坑清单、疑点）
- `references/10-console-system-detailed.md` —— 控制台/系统/文件/网络/加密 **逐条实测详解**（31KB：控制台 API 与颜色、信息框与延时框、文件与目录、进程与模块、注册表/时间/内存/环境/磁盘、Winsock、MD5/SHA1、实用示例、未验证清单）
- `references/11-ai-runtimes-onnx-gguf.md` —— ★**AI 运行时**：64 位指针规则（`整数` 是 32 位、要用 `大数`）、外部 DLL 调用链、**ONNX Runtime 完整推理（已跑通）**、**GGUF 量化模型解析 + llama.cpp 载入（已跑通）**、配套偏移表与坑清单
- `references/12-gui-app-practice.md` —— ★**完整 GUI 应用实战**（2026-09 用极语言复现 aardio《离线翻译工具》实测）：可用的 GUI 骨架、★**控件内容读写官方 `~` 运算符**、**SysListView32 完全指南（LVM 常量表 / 列结构 / 项结构 / 加行 / 取选中）**、组合框、剪贴板、自建 JSON（只构造不解析）、`创建进程` 10 参启动外部程序、GUI 程序里的 Winsock 与「定长前缀」协议、**11 条新增致命陷阱（含 193/0xC0000005/0xC0000409 最小复现）**、产物加载校验、GUI 自动化测试技巧
- 实测工作区（笔记与全部示例）：`D:\SEC\deepseek-sec\`（`notes\`、`examples\`、`shots\`、`docs\`、`tools\`）
- 示例索引：`examples/01..08-*`（核心速查/应用/GUI/进阶，主 agent 实测）、`examples/09-bas-1..8`（基础语法）、`examples/10-adv-1..13`（进阶 12 通过 + `90-adv-12-expected-fail` 反例）、`examples/11-*`（TCP 连接探测 + 端口扫描 + 目录清单）、`examples/12-gui-*`（GUI 4 例 + 截图 + Edit 复验）、`examples/13-aux-*`（控件常量表/编号规律）、`examples/14-sys-1..8`（控制台能力/信息框/文件目录/进程模块/时间内存环境磁盘/网络/MD5·SHA1/实用示例）、★`examples/20-ai-onnx-run.txt`（ONNX Runtime 完整推理）、`examples/21-ai-gguf-parse.txt`（纯极语言 GGUF 解析，32 位产物）、`examples/22-ai-gguf-llama.txt`（llama.dll 载入 GGUF）、★`examples/30-app-net-json.txt`（JSON 构造 + GBK↔UTF-8 + Winsock 连接探测，**已复验 BUILD_OK 且运行输出符合预期**）、★`examples/31-gui-control-io.txt`（GUI `~` 读写控件 + 组合框 + 四列 ListView 加行 + 剪贴板写入读回，**已复验 BUILD_OK + 截图 `31-gui-control-io-shot.png`**）

速记：
- 类型：`布尔 字符 字节 短数/整形 正短/正数 整数 文本 小数/浮点 双精 大数 日期 变数`（`文本` 是定长缓冲区，`文本 名[64]`）
- 数组：固定 `整数 数组(5)`（或 `[5]`），索引 `数组(0)=10`；动态 `整数 数组();` + `重置 数组(n)`
- 控制流：`如果(){} 否则{}`、`判断(x) 为 a{} 为 b,c{} 为 <5{} 否则{}`、`循环(3){}`、`循环{}`、`循环(条件){}`、`循环于(i=0;i<n;i++){}`、`循环在(条件){}`、`跳出`、`跳到 标记`、`跳转>标记`、`如果是(条件,a,b)`
- 函数：`程序 名称(参数,参数2) ... 返回(值) 结束`（参数按值传递；`返回` 可直接返回，浮点结果用 `返回(:表达式)`）
- 指针/内存：`变量(偏移)` 读写 1 字节、`变量(偏移)%` 2 字节、`变量(偏移)&` 4 字节、`变量(偏移)!` 4 字节浮点；★64 位产物下**不能用无后缀的 `变量(偏移)`（必崩）**，要用 `(偏移)$`=8 字节大数 / `&` / `%` / `!` / `#`，而且**API 返回的指针必须存 `大数`/`变数`**（`整数` 是 32 位会截断）；`整数 p=文本变量;` 取缓冲区地址；`整数 p="字面量";` 字面量即地址
- C 运行库（需 `模块文件=CMD.inc`）：`写文字`(puts) `写字符`(putchar) `写格式`(printf) `读字符` `读格式` `命令行`(system) `内存复制` 以及 `开文件/读文件/写文件/关文件/流定位` 等（中文名见 `references/05-console-system.md`）
- 内置模块：控制台 `CMD.inc`；GUI `GUI.inc`（**入口 `程序 程序入口` 由 GUI.inc 提供**，用户只写 `程序 加载窗体` + `程序 窗体消息`）；需要 Win32/Winsock 时在模块列表里加 `dll.inc`。更多模块见 `D:\SEC\inc\`
- 命令行参数：`整数 s=启动参数;`（Win32 `GetCommandLineA`）拿到完整命令行，自己扫描解析即可（实测 `04-app-baseconv.txt` 用 `app-baseconv.com 255` 得到 `十六进制=FF`）；`程序 初始启动(argc,argv)` 这种带参入口**不可用**（会卡死）
- 延时：`延时(毫秒);` 可用（实测编译运行通过）
- 字符串字面量里 `\r\n` 等转义可用：`写文字("A\r\nB\r\n");` 正常换行（`\t` 在格式串里不生效）

## 五、资料与工作区

- 官方文档原始文件：`D:\SEC\sec.htm`（语言手册）、`lib.htm`（C 运行库/API 中文函数表）、`obj.htm`（内置对象/控件）、`msapi.htm`（Windows API 大全）
- **已转成 UTF-8 纯文本便于检索**：`D:\SEC\deepseek-sec\docs\sec.clean.txt`（12492 行，分节：第一节入门 469、变量类型 639、转义标点 841、运算符 919、编程语法 997、文本数组 1286、指针列举 1403、机器指令 1629、跳转标记 1683、结构体 1830、数据表 1944、子类对象 2083、常量 2253、信息框 2343）等
- 官方示例源码：`D:\SEC\code\*.sec`（GBK 文本 + 二进制头尾；读法 `[Text.Encoding]::GetEncoding('GB18030').GetString([IO.File]::ReadAllBytes($p))`，注意其中的 `小程序/程序段` 是本版编译器**不支持**的旧关键字）
- 实测工作区：`D:\SEC\deepseek-sec\`（`tools\` 工具脚本、`examples\` 可运行示例、`notes\` 学习笔记、`shots\` 截图、`docs\` 文档转文本）
- 本技能自带：`scripts/sec.ps1`（编译+运行+自动纠正 GBK/CRLF+失败判定+重试）、`scripts/build-check.ps1`（★**编译后再用 `CreateProcess` 真校验产物能否被加载**，能抓出「`BUILD_OK` 但 193 坏镜像」；`-RunSeconds N` 顺带做 GUI 保活测试）、`scripts/shot.ps1`（窗口截图）、`scripts/winprobe.ps1`（枚举目标进程子窗口 / BM_CLICK / WM_SETTEXT / 读窗口标题，GUI 调试用）、`scripts/utf8-to-gbk.ps1`（UTF-8 源码 → GBK+CRLF）
- `examples/*.txt` 全部经 `scripts/sec.ps1` 复验：**46/46 BUILD_OK**（另有 1 个 `90-*-expected-fail.txt` 故意复现编译错误）；本次新增的 `examples/20-ai-onnx-run.txt`、`21-ai-gguf-parse.txt`、`22-ai-gguf-llama.txt`、`30-app-net-json.txt`、`31-gui-control-io.txt` **均已复验 BUILD_OK**（30 验了运行输出、31 验了运行 + 截图）

## 五之二、实战工程案例

★ **`D:\SEC\project\ji-offline-translator\`** —— 用极语言复现 aardio 工程《离线翻译工具》的完整成品，
可作为「写一个真能用的极语言 GUI 应用」的参照：

- 前端：极语言 GUI（约 780 行）；引擎：外挂 Python（CTranslate2 / PaddleOCR / Qwen / SQLite），
  用 `创建进程` 拉起、Winsock 走**裸 TCP 定长前缀协议**通信；
- 功能：英中互译、AI 解析（7 种任务）、历史列表增删、剪贴板、语言交换；
- 配套文档 `README.md` 里有**功能对照表、已知限制、以及一份面向极语言的踩坑清单**（与本技能 §三 + 参考 12 互为印证）；
- 编译校验请用 `scripts/build-check.ps1`：本项目的 `translator.txt` 实测 `LOAD_OK`。

## 六、调试流程建议

1. 编译报 `未定义的名称:X` → 检查配置行顺序、关键字拼写（本版没有 `小程序`/`程序段`）、是否需要 `模块文件=CMD.inc`。
2. 报 `语法有误` → 检查该行上一行是否缺少 `;`/换行、`判断` 是否多写了 `结束`。
3. 报 `内部错误 -> Invalid procedure call or argument` → 优先怀疑：LF 换行、`a%b`、`函数` 定义辅助函数、某些语句组合过复杂（拆成多个小程序二分定位）。
4. `BUILD_OK` 但程序卡死 → 用 `shot.ps1` 截出控制台窗口看真实输出停在哪儿；优先怀疑 `文本=字面量`（运行期/例程内）、结构体数组、子类带参方法，先试 `定义 位数=64;`。
5. 需要看输出但 stdout 被吞 → 让程序写文件，或用截图看控制台窗口。
6. ★ `BUILD_OK` 但**运行不了 / 报 193 不是有效 Win32 程序** → 编译产物是坏镜像。改用 `scripts/build-check.ps1` 复验；确认是坏镜像就把「扫描/解析类」过程拆小或换写法重编（同一份源码反复重编有时会变好）。
7. ★ 程序**跑几秒后自己消失**（无任何输出）→ 先拿退出码：`0xC0000005`＝踩了「条件里调过程 / 参数指针加减」；`0xC0000409`＝栈缓冲溢出，**定时器场景下几乎都是 `设置定时` 第 4 参数传了 `@过程`**（见 §三，改成传 `0` + `为 定时事件` 分支即好，与“回调里做多层调用”无关）。对照 §三 的几条逐一排除。
8. ★ GUI 程序**界面点不动 / 状态栏不刷新** → 多半是消息循环被阻塞（长时间 `循环 + 延时` 轮询）。先把阻塞换成惰性读取。
9. ★★ **报错行号不可靠时怎么找真正的坏行**（`语法有误 + 内部错误` 的行号常比真凶小 1~2 行，甚至报到下一个过程里）：
   1) 先把可疑新代码**追加到一份已知可编译的基线**上，并给新过程的**名字统一加前缀**（如 `X_`）——
      这样不会出现 `未定义的名称` 提前中断编译、掩盖真正的解析错误（实测：不这么做时“丢一个过程”的对照实验全部无效）；
   2) 再按块二分：只保留前 N 个过程编译一次，N 逐段加大，第一次失败的那一段就是毒源；
   3) 落到单个过程后，用**语句前缀**二分（只保留前 k 条语句）定位到具体语句；
   4) 定位到语句后优先查**符号名**：把常量当函数调用（`显示窗口(…)`）、过程名与全局变量重名、
      `如果` 体内嵌 `如果` 后跟语句——这三类都会给出错位的行号。
   现成脚本：`D:\SEC\project\ji-offline-translator\tools\brem.ps1`（整过程增删）、`bz.ps1`（清空过程体）。
9. ★ 自动化测试里「程序读不到外部灌进去的文字」→ 不是程序的锅：外部 `SetWindowTextA` 灌的字本进程 `~` 读不到，改用 `PostMessage` 发 `WM_CHAR` 模拟键入。

## 七、AI 运行时（ONNX Runtime / GGUF 量化模型）

详细实测笔记见 `references/11-ai-runtimes-onnx-gguf.md`；可运行示例 `examples/20-ai-onnx-run.txt`、`21-ai-gguf-parse.txt`、`22-ai-gguf-llama.txt`；OrtApi 偏移表生成器 `scripts/ort-vtable-offsets.py`。

**实测结论（2026-09，本机 Windows x64）**

- ✅ **ONNX**：极语言能载入 `onnxruntime.dll` 并**完整跑通一次推理**（建环境→建会话→建张量→`Run`→读输出），示例 20 输出 `40000000 40400000 …` 即 2,3,4,5,6,7。
- ✅ **GGUF**：**不依赖任何 DLL**、纯极语言即可解析 GGUF 头部/元数据/张量表（示例 21：GGUF v3、320 张量、`general.file_type=15`=Q4_K_M、词表 248320）；用 llama.cpp 的 `llama.dll` 也能把 Q4_K_M 模型真正读进内存（示例 22）。
- ⚠️ GGUF 的**完整文本生成**还要自建 `llama_batch`/`llama_sampler_chain` 等结构体，成本高 → **推荐极语言只当外壳，推理交给本地 ollama（127.0.0.1:11434）/ llama-server 的 HTTP API**（Winsock 见 `05/10` 笔记）。

**三条铁律**（少一条就跑不起来）

1. `定义 位数=64;` —— AI 的 dll 都是 x64，32 位产物载不进去。
2. 指针一律用 `大数`/`变数` 保存 —— `整数` 即使 64 位产物也是 32 位，会把地址截断。
3. 调函数指针用 `调用 变量(参数...)`；**直接**调导入的中文 API 写 `名称(参数);`（加 `调用` 会报"符号不存在"）。

最小骨架（等价于示例 20 的前半段，实测通过）：

```ji
程序类型=2
模块文件=CMD.inc
定义 位数=64;
引入 "lib\kernel32.lib";
导入 载库 别名 LoadLibraryA 支持库 "KERNEL32.DLL",1;
导入 取址 别名 GetProcAddress 支持库 "KERNEL32.DLL",2;
导入 冲刷 别名 fflush 支持库 "MSVCRT.DLL",1;
程序 初始启动
大数 库=载库("C:\...\onnxruntime.dll");
大数 取基=取址(库,"OrtGetApiBase");
大数 基=调用 取基();
大数 取接口=基(0)$;                 // 结构体第 0 个字段就是 GetApi
大数 接口=调用 取接口(23);           // 23 = ORT_API_VERSION，必须与运行时版本匹配
大数 建环境=接口(24)$;               // OrtApi 里 CreateEnv 的偏移
大数 环境=0;
大数 状态=调用 建环境(2,"app",@环境);  // 返回 0 = 成功
冲刷(0);退出(0);
结束
```

> 提示：`onnxruntime.dll` 只导出 `OrtGetApiBase` 等极少数符号，其余几百个 API 全在 `OrtApi` 函数指针表里（偏移 = 序号×8），用 `scripts/ort-vtable-offsets.py` 对任意版本头文件重新生成即可。

## 八、用极语言写「完整 GUI 应用」的推荐做法（实战结论）

详细内容与可复制代码见 `references/12-gui-app-practice.md`，这里只列**决策要点**：

1. **控件内容读写一律用官方 `~` 运算符**：读 `文本变量 ~ 控件;`，写 `控件 ~ 内容;`。
   `改控件字` 只适合 Static/Button，对某些 Edit 会崩。
2. **列表视图用 `SysListView32` + LVM 消息**；常量表见参考 12 §3.2
   （插入列 `$101B`、插入项 `$1007`、写子项 `$102E`、取子项 `$102D`），
   结构体字段顺序必须与 `LVCOLUMNA`/`LVITEMA` 一致。
3. **组合框用索引分发逻辑**，别做字符串比较：`组合添加` 加项、`$14E` 置选中、`$147` 取选中。
4. **每个 `程序` 的局部声明写在最前面**；不要在有局部声明之后再写语句。
5. **不在 `如果(...)` 条件里调用自定义过程**；不在过程里对参数做指针加减
   → 需要拼接就用全局缓冲 + `格式化(基底,"%s%s",前缀,尾部)`。
6. **别用「定时器回调」串联长流程**（网络→写库→刷 UI 会 0xC0000409）。
   定时器只用来做「置标志 + 短动作」，重活放在按钮事件里。
7. **网络通信**：GUI.inc 已带 `ws2_32`。在 GUI 程序里**不要拼 HTTP 头**（发格式化缓冲会 -1）；
   与他人进程通信优先用**裸 TCP + 8 字节十进制长度前缀**，收发都用全局缓冲。
8. **与外部程序（Python/Node…）协同时，让外部程序适配极语言**，而不是让极语言解析 JSON：
   外部进程把结果转成「纯文本行协议」，极语言只按字节读——这比在极语言里写 JSON 解析器
   可靠得多（后者还会把编译器搞出坏镜像）。
9. **编译后必须校验产物**：`powershell -File scripts/build-check.ps1 -Src app.txt -RunSeconds 5`。
10. **自动化回归**：用 `PostMessage WM_CHAR` 模拟输入、`PostMessage WM_COMMAND(0x0111, 控件ID, 0)`
    点按钮、`shot.ps1` 截图；读控件文字用 ANSI 版 API + GBK 解码。
