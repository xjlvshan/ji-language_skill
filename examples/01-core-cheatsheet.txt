程序类型=2
模块文件=CMD.inc
程序 初始启动
整数 a=7;
整数 b=2;
整数 m=a 余 b;
写格式("a+b=%d a-b=%d a*b=%d a/b=%d mod=%d\r\n",a+b,a-b,a*b,a/b,m);
整数 sq=平方(a);
写格式("平方(a)=%d\r\n",sq);
浮点 f=2.5;
双精 d=1.25;
文本 buf[128];
buf="f="+f+" d="+d;
写文字(buf);写字符(13);写字符(10);
文本 名字[32];
内存复制(名字,"极语言",7);
写文字(名字);写字符(13);写字符(10);
整数 数组(5);
数组(0)=10;数组(1)=20;数组(4)=50;
写格式("数组=%d,%d,%d\r\n",数组(0),数组(1),数组(4));
文本 内容[64];
内存复制(内容,"0123456789ABCDEF",17);
写格式("内容(2)=%d\r\n",内容(2));
写格式("如果是=%d\r\n",如果是(a>2,111,222));
写格式("字面量=%s\r\n","hello");
结束
程序 平方(数)
返回(数*数)
结束