# examples · 全部实测可编译可运行

运行方式（本目录或任意目录）：

```powershell
powershell -File ..\scripts\sec.ps1 -Src <本目录>\01-core-cheatsheet.txt -Run
```

| 文件 | 内容 | 实测结果 |
|---|---|---|
| `01-core-cheatsheet.txt` | 变量/算术/取模/浮点转文本/文本缓冲区/数组/成员与字面量 | `BUILD_OK`，输出 `a+b=9 ... mod=1`、`f=2.500000 d=1.250000`、`数组=10,20,50`、`内容(2)=50`、`如果是=111` |
| `02-flows-cheatsheet.txt` | 四种循环 + 跳出 + 如果/否则 + 如果是 + 判断链 | `BUILD_OK`，输出循环序列与 `zero-one` |
| `03-app-mandelbrot.txt` | ASCII 曼德博集合（浮点运算 + 字节级拼字符串） | `BUILD_OK`，渲染 22×78 图形并 `done.` |
| `04-app-baseconv.txt` | 命令行进制转换器（`启动参数` 取命令行 + 逐字节扫描） | `BUILD_OK`；`app-baseconv.com 255` → `十六进制=FF`、`二进制=11111111` |
| `05-app-algorithms.txt` | 冒泡排序 + 100 以内素数筛 + 斐波那契（数组/全局/函数/嵌套循环） | `BUILD_OK`，排序结果与素数 25 个、斐波那契前 20 项全部正确 |
| `06-app-fileinfo.txt` | 自包含文件读写演示（`建文件/写文件` 造文件 → `开文件/文件大小/读文件/关文件/申请内存/释放内存` 读回 + 逐字节统计） | `BUILD_OK`，输出 `写入字节=31 / 字节数=31 已读=31 / 行数=3 非空白字符=23` 并打印内容 |
| `07-gui-minimal.txt` | 最小 GUI 骨架（GUI.inc 入口 + 主窗口/标签/按钮/编辑框 + 消息过程） | `BUILD_OK`，截图确认窗口「窗口标题」含标签/按钮/编辑框 |
| `08-struct-table-class-dll.txt` | 结构体 + 数据表 + 子类成员 + DLL 导入（`GetSystemMetrics`） | `BUILD_OK`，输出 `结构: 生命=100 … 名字=赵子龙 / 数据表: 生命=500 等级=9 / 子类: 值=77 等级=3 / DLL: 屏幕宽=1920` |
| `bas-*` `adv-*` `sys-*` `gui-*` | 分领域实测示例（基础语法 / 进阶 / 控制台与系统 / GUI，见 `references/`） | 见对应笔记中的实测记录 |

## 分领域示例（agent 实测 + 主 agent 复验 BUILD_OK）

| 文件 | 主题 | 关键输出 |
|---|---|---|
| `09-bas-1..8` | 程序结构与入口 / 类型与转换 / 转义标点 / 运算符 / 分支 / 循环跳转 / 文本数组 / 浮点转文本专题 | `加法(3,4)=7`、`数 余 值=1`、`双精 %f=3.125000`、`检索(数列,9,8)=2` 等 |
| `10-adv-1..13` | 指针内存 / 检索与宽度 / 结构体 / 数据表 / 常量与中括号 / 跳到跳转 / 机器指令 / DLL 模板 / MessageBoxA / 结构体文本成员 / 子类 / 列举 | `@m=402000`、`检索(m,67,32)=2`、`[6+[35-12]*150-[60/12]]=4345`、`1..100 和=5050`、`机器加法(3,4)=7`、`GetTickCount=···` |
| `90-adv-12-expected-fail.txt` | **预期失败**反例（`定义 名称=数值;`） | 复现 `内部错误 -> Subscript out of range` |
| `adv-9-messagebox.png` | MessageBoxA 弹框截图证据 | 标题 `adv-9` + 正文 + 确定按钮 |
| `12-gui-1..4.txt` | GUI.inc 骨架 / 常用控件 / 菜单+定时器 / GDI 绘图 | 全部 BUILD_OK，配套截图 `12-gui-N-shot.png`（实看：标题、控件、列表项、进度条、菜单、绘图） |
| `12-gui-5-edit-settext.txt` | 复验 `改控件字` 对 Edit 是否有效 | BUILD_OK，截图显示编辑框内容被改成「改控件字后来设置」 |
| `13-aux-1.txt` | 控件消息常量表（WM_/BN_/EN_/EM_/LB_/CB_/PBM_/SBM_/BM_） | BUILD_OK，按手册值打印 |
| `13-aux-2.txt` | 控件编号/通知码编码规律 + ID 规划表 | BUILD_OK：`50331650 → 编号2/通知768`、`2 → 编号2/通知0` |
| `11-app-netport.txt` | 本机端口扫描器（TCP connect 探测 6 个端口） | BUILD_OK：`3080:0 / 3306:-1 / 5432:-1 / 8080:-1 / 135:0 / 445:0`，开放端口数=3 |
| `11-net-tcp-connect.txt` | 最小 TCP 连接探测（`网络启动/分配/连网/关闭/网络终止`） | BUILD_OK：`连 127.0.0.1:3080 = 0`、`连 127.0.0.1:9 = -1` |
| `11-app-filelist.txt` | 目录清单（`查找文件/下个文件/关闭查找`） | BUILD_OK：列出当前目录 `*.txt` 并统计（验证时 `共 74 个文件`） |
| `14-sys-1..8.txt` | 控制台完整能力 / 信息框与延时框 / 文件与目录 / 进程与模块 / 时间·内存·环境·磁盘 / 网络 TCP / MD5·SHA1 / 实用示例 | 全部 BUILD_OK 且 `[exit=0]`，逐条真实输出见 `references/10-console-system-detailed.md` |
| `14-sys-console-shot.png` | 控制台彩色 + 光标定位输出截图证据 | 6 色文本 + `改光标位` 定位 + 白底黑字 |

另有 `11-sys-*`（控制台/系统/文件）来自对应领域，见该领域笔记。

注意：`.com` 是控制台产物（真 32 位 PE），`.exe` 是 GUI 产物；编译产物会写在源码同目录，可随时删除。
首次运行刚编译的程序偶发假死，重跑一次即可（`sec.ps1 -Run` 会自动重试）。
