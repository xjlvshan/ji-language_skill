程序类型=2
模块文件=CMD.inc
// ============================================================
// sys-1 极语言控制台完整能力演示（可重定向，退出码 0）
// 注意：入口必须是源码里第一个 程序/函数
// ============================================================
文本 标题[64];
文本 行[8];
整数 输出;
整数 输入;
整数 错误;
整数 模式;
程序 初始启动
	输出=取控制台(标准输出);
	输入=取控制台(标准输入);
	错误=取控制台(标准错误);
	写文字("=== 1. 写文字 / 写字符 / 写格式 ===");
	写文字("(写文字自动换行)");
	写字符(65);写字符(66);写字符(67);写字符(10);
	写格式("整数=%d 十六进制=0x%X 八进制=%o",255,255,255);写字符(10);
	写格式("负数=%d 无符号=%u 字符=%c",-7,-7,90);写字符(10);
	写格式("左对齐=[%-8d] 补零=[%08d] 前缀=[%#x]",42,42,36);写字符(10);
	写格式("浮点字面量=%f (错,见笔记)",3.5);写字符(10);
	浮点 数=3.5;
	行="浮点变量拼文本="+数;
	写文字(行);
	写格式("百分号=50%%");写字符(10);
	复制文字(行,"abcdefg");
	写格式("字符串=[%s] 截断=[%.4s]",行,行);写字符(10);
	写文字("");
	写文字("=== 2. 改字属性（整屏字符颜色，高亮=+8） ===");
	改字属性(输出,12);写文字("亮红=12");
	改字属性(输出,10);写文字("亮绿=10");
	改字属性(输出,11);写文字("亮青=11");
	改字属性(输出,13);写文字("亮紫=13");
	改字属性(输出,14);写文字("亮黄=14");
	改字属性(输出,9);写文字("亮蓝=9");
	改字属性(输出,7);写文字("普通灰=7");
	写文字("=== 3. 改光标位（高16位=行 低16位=列） ===");
	改光标位(输出,8*65536+30);
	改字属性(输出,15);写文字("<<第8行第30列");
	改光标位(输出,9*65536);写字符(10);
	改字属性(输出,112);写文字(" 白底黑字=0x70 ");
	改字属性(输出,7);写字符(10);
	写字符(10);
	写文字("=== 4. 设置标题 / 获取标题 ===");
	写格式("设置标题返回=%d",设置标题("sys-1 控制台能力演示"));写字符(10);
	写格式("获取标题返回=%d 标题=[%s]",获取标题(标题,64),标题);写字符(10);
	写字符(10);
	写文字("=== 5. 控制台句柄 / 码页 / 输入模式 ===");
	写格式("标准输入=%d 标准输出=%d 标准错误=%d",输入,输出,错误);写字符(10);
	写格式("输入码页=%d 输出码页=%d",控制码页,输出码页);写字符(10);
	写格式("命令窗口=%d 进程标识=%d 开机毫秒=%d",命令窗口,进程标识,开机毫秒);写字符(10);
	获取模式(输入,@模式);
	写格式("输入模式=0x%X 设置模式返回=%d",模式,设置模式(输入,模式));写字符(10);
	写格式("控制大小(设缓冲区)返回=%d",控制大小(输出,25*65536+80));写字符(10);
	写字符(10);
	写文字("=== 6. 命令行（system） ===");
	命令行("title 命令行改过的标题");
	写文字("命令行 title 已执行");
	写字符(10);
	写文字("=== 7. 会阻塞的函数（批处理中禁止调用） ===");
	写文字("读字符/读文字/读格式/读控制台/分控制台 都会等待或改变控制台，实测会挂起");
	写文字("--- sys-1 演示结束 ---");
	退出(0);
结束