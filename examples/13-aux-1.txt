程序类型=2
模块文件=CMD.inc
程序 初始启动
写文字("== 极语言 控件消息常量表（实编译校验） ==\r\n");
写文字("-- 窗口消息 --\r\n");
写格式("窗口关闭 WM_CLOSE        = $%X\r\n",$10);
写格式("命令事件(单击左键) WM_COMMAND = $%X\r\n",$111);
写格式("定时事件 WM_TIMER        = $%X\r\n",$113);
写格式("水平滚动 WM_HSCROLL      = $%X\r\n",$115-1);
写格式("垂直滚动 WM_VSCROLL      = $%X\r\n",$115);
写格式("绘制按钮 WM_CTLCOLORBTN  = $%X\r\n",$135);
写格式("绘制静态 WM_CTLCOLORSTATIC= $%X\r\n",$138);
写格式("鼠标移动 WM_MOUSEMOVE    = $%X\r\n",$200);
写格式("按下左键 WM_LBUTTONDOWN  = $%X\r\n",$201);
写格式("释放左键 WM_LBUTTONUP    = $%X\r\n",$202);
写格式("拖放文件 WM_DROPFILES    = $%X\r\n",$233);
写文字("-- 按钮通知码 BN_ --\r\n");
写格式("按钮单击 BN_CLICKED  = %d\r\n",0);
写格式("按钮双击 BN_DBLCLK   = %d\r\n",5);
写格式("按钮焦点 BN_SETFOCUS = %d\r\n",6);
写文字("-- 编辑框通知码 EN_ --\r\n");
写格式("编辑获得焦点 EN_SETFOCUS  = $%X\r\n",$100);
写格式("编辑失去焦点 EN_KILLFOCUS = $%X\r\n",$200);
写格式("编辑内容更改 EN_CHANGE    = $%X\r\n",$300);
写文字("-- 编辑框消息 EM_ --\r\n");
写格式("编辑选中 EM_SETSEL      = $%X\r\n",$B1);
写格式("编辑替换 EM_REPLACESEL  = $%X\r\n",$C2);
写格式("编辑取行 EM_GETLINE     = $%X\r\n",$C4);
写格式("编辑只读 EM_SETREADONLY = $%X\r\n",$CF);
写文字("-- 列表框消息 LB_ --\r\n");
写格式("列表添加 LB_ADDSTRING  = $%X\r\n",$180);
写格式("列表删除 LB_DELETESTRING= $%X\r\n",$182);
写格式("列表清除 LB_RESETCONTENT= $%X\r\n",$184);
写格式("列表选项 LB_GETCURSEL  = $%X\r\n",$188);
写格式("列表数量 LB_GETCOUNT   = $%X\r\n",$18B);
写格式("列表文件 LB_DIR        = $%X\r\n",$18D);
写文字("-- 组合框消息 CB_ --\r\n");
写格式("组合添加 CB_ADDSTRING = $%X\r\n",$143);
写格式("组合总项 CB_GETCOUNT  = $%X\r\n",$146);
写格式("取组合项 CB_GETCURSEL = $%X\r\n",$147);
写格式("组合读取 CB_GETLBTEXT = $%X\r\n",$148);
写文字("-- 进度条消息 PBM_ --\r\n");
写格式("进度范围 PBM_SETRANGE   = $%X\r\n",$401);
写格式("进度位置 PBM_SETPOS     = $%X\r\n",$402);
写格式("进度步长 PBM_SETSTEP    = $%X\r\n",$404);
写格式("进度步进 PBM_STEPIT     = $%X\r\n",$405);
写格式("进度限定 PBM_SETRANGE32 = $%X\r\n",$406);
写格式("进度取值 PBM_GETPOS     = $%X\r\n",$408);
写文字("-- 滚动条消息 SBM_ --\r\n");
写格式("滚动位置 SBM_SETPOS = $%X\r\n",$E0);
写格式("滚动取值 SBM_GETPOS = $%X\r\n",$E1);
写格式("滚动限定 SBM_SETRANGE= $%X\r\n",$E2);
写文字("-- 按钮消息 BM_ --\r\n");
写格式("获取选择 BM_GETCHECK = $%X\r\n",$F0);
写格式("设置选择 BM_SETCHECK = $%X\r\n",$F1);
写格式("模拟点击 BM_CLICK    = $%X\r\n",$F5);
写文字("-- 剪贴板消息 --\r\n");
写格式("内容剪切 WM_CUT   = $%X\r\n",$300);
写格式("内容复制 WM_COPY  = $%X\r\n",$301);
写格式("内容粘贴 WM_PASTE = $%X\r\n",$302);
写文字("以上数值均取自 sec.clean.txt 手册常量表，本程序编译运行用于校验 %%X 输出可用。\r\n");
结束