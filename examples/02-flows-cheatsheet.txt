程序类型=2
模块文件=CMD.inc
程序 初始启动
整数 i;
写文字("循环于: ");
循环于(i=0;i<3;i++){写格式("%d ",i);}
写字符(13);写字符(10);
写文字("循环(2): ");
循环(2){写字符(42);}
写字符(13);写字符(10);
写文字("循环在: ");
i=0;
循环在(i<3){写格式("%d ",i);i++;}
写字符(13);写字符(10);
写文字("循环+跳出: ");
i=3;
循环{写格式("%d ",i);i--;如果(i=0){跳出;}}
写字符(13);写字符(10);
写文字("如果/否则: ");
如果(i<5){写文字("lt5");}
否则{写文字("ge5");}
写字符(13);写字符(10);
写文字("判断: ");
判断(i)
为 0,1{写文字("zero-one");}
为 2{写文字("two");}
否则{写文字("other");}
结束