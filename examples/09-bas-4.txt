程序类型=2
模块文件=CMD.inc

程序 初始启动
	写文字("== bas-4 运算符 ==");
	整数 数=7,值=2;
	写格式("符号算术: 数+值=%d\r\n",数+值);
	写格式("符号算术: 数-值=%d\r\n",数-值);
	写格式("符号算术: 数*值=%d\r\n",数*值);
	写格式("符号算术: 数/值=%d （整数除法取整）\r\n",数/值);
	写格式("中文算术: 数 加 值=%d\r\n",数 加 值);
	写格式("中文算术: 数 减 值=%d\r\n",数 减 值);
	写格式("中文算术: 数 乘 值=%d\r\n",数 乘 值);
	写格式("中文算术: 数 除 值=%d\r\n",数 除 值);
	写格式("中文算术: 数 余 值=%d （取模只能用中文）\r\n",数 余 值);
	写格式("中文位运算: 数 或 值=%d\r\n",数 或 值);
	写格式("中文位运算: 数 与 值=%d\r\n",数 与 值);
	写格式("中文位运算: 数 异 值=%d\r\n",数 异 值);
	写格式("中文位运算: 反 数=%d  非 数=%d\r\n",反 数,非 数);
	写格式("中文位运算: 数 右移 值=%d\r\n",数 右移 值);
	写格式("字面量符号位运算: 1|2=%d  1&3=%d  1^3=%d  ~1=%d\r\n",1|2,1&3,1^3,~1);
	写格式("字面量移位: 8>>1=%d  1<<3=%d\r\n",8>>1,1<<3);
	写格式("优先级: 1+2*3=%d\r\n",1+2*3);
	写格式("优先级: (1+2)*3=%d\r\n",(1+2)*3);
	整数 乙=10;
	乙+=5;
	写格式("复合赋值 乙+=5 -> %d\r\n",乙);
	乙-3;
	写格式("省略等号 乙-3 -> %d\r\n",乙);
	乙*2;
	写格式("省略等号 乙*2 -> %d\r\n",乙);
	写文字("比较运算不能当表达式用，只能出现在 如果/判断/循环/如果是 的条件里：");
	如果(数>值){写文字("  数>值 成立");}
	如果(数>=值){写文字("  数>=值 成立");}
	如果(数<>值){写文字("  数<>值 成立");}
	如果(数!=值){写文字("  数!=值 成立");}
	如果(数=值){写文字("  数=值 成立");}否则{写文字("  数=值 不成立");}
	写格式("如果是(数>值,100,200) = %d\r\n",如果是(数>值,100,200));
	写格式("如果是(数<值,100,200) = %d\r\n",如果是(数<值,100,200));
	写文字("程序结束");
结束
