程序类型=2
模块文件=CMD.inc
// sys-5 时间 / 内存 / 环境变量 / 磁盘（逐条实测通过）
// 另：注册表函数的中文名与坑见笔记第三节（查询值项/改子项值 都是 6 参数）
文本 值[256];
文本 数值[512];
文本 标签[16];
整数 表项;
整数 类型;
整数 大小;
整数 缓冲;
整数 数1;
整数 数2;
整数 数3;
整数 数4;
时间类 现在;
整数 秒;
程序 初始启动
	写文字("=== 1. 系统时间：当前时间(时间类 16 字节) ===");
	当前时间(现在);
	写格式("本地 年=%d 月=%d 日=%d 时=%d 分=%d 秒=%d 星期=%d",现在.年,现在.月,现在.日,现在.时,现在.分,现在.秒,现在.星期);写字符(10);
	国标时间(现在);
	写格式("国标(UTC) 年=%d 月=%d 日=%d 时=%d",现在.年,现在.月,现在.日,现在.时);写字符(10);
	写文字("=== 2. C运行库时间 ===");
	取时间(@秒);
	写格式("取时间(整数秒)=%d",秒);写字符(10);
	写文字("转时间=");写文字(转时间(@秒));
	写字符(10);
	写字符(10);
	写文字("=== 3. 内存：申请/填充/复制/比较/传送/释放 ===");
	缓冲=申请内存(64);
	填充内存(缓冲,65,16);
	写格式("填充内存后 缓冲(0)=%d 缓冲(15)=%d",缓冲(0),缓冲(15));写字符(10);
	数1=申请内存(64);
	内存复制(数1,缓冲,16);
	写格式("内存比较(相同16字节)=%d (0=相同)",内存比较(缓冲,数1,16));写字符(10);
	填充内存(数1,66,16);
	写格式("内存比较(不同)=%d (-1=不同)",内存比较(缓冲,数1,16));写字符(10);
	内存传送(数1,缓冲,8);
	写格式("内存传送后 数1(0)=%d",数1(0));写字符(10);
	释放内存(缓冲);
	释放内存(数1);
	写文字("  两块内存已释放");
	写格式("可用的中文内存函数：申请内存/申零内存/保留内存/释放内存/填充内存/内存复制/内存传送/内存比较/查找字节");
	写字符(10);
	写字符(10);
	写文字("=== 4. 环境变量：取环境量 / 展开环境 ===");
	写格式("取环境量(TEMP) 返回=%d",取环境量("TEMP",值,256));写字符(10);
	写文字("  TEMP=");写文字(值);
	写格式("展开环境(%TEMP%) 返回=%d",展开环境("%TEMP%",值,256));写字符(10);
	写文字("  TEMP=");写文字(值);
	写字符(10);
	写文字("=== 5. 磁盘：分区容量 / 分区空间 ===");
	写格式("分区容量(D:) 返回=%d",分区容量("D:\\",@数1,@数2,@数3));写字符(10);
	写格式("  可用=%u 总=%u 剩余=%u",数1,数2,数3);写字符(10);
	写格式("分区空间(D:) 返回=%d",分区空间("D:\\",@数1,@数2,@数3,@数4));写字符(10);
	写格式("  每簇扇区=%d 扇区字节=%d 簇数=%d 可用簇=%d",数1,数2,数3,数4);写字符(10);
	写字符(10);
	写文字("=== 6. 机器/运行时信息 ===");
	写格式("开机毫秒=%d 进程标识=%d 命令窗口=%d",开机毫秒,进程标识,命令窗口);写字符(10);
	写文字("--- sys-5 演示结束 ---");
	退出(0);
结束