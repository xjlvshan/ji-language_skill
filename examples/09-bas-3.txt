程序类型=2
模块文件=CMD.inc

程序 初始启动
	写文字("== bas-3 转义符号与标点 ==");
	写文字("1. \r\n 换行可用: A\r\nB");
	写文字("2. \t 制表符不可用（原样输出反斜杠t）: tab[\t]end");
	写文字("3. 反斜杠转义不可用（原样输出两个反斜杠）: a\\b");
	写文字("4. 两个单引号 '' 表示一个双引号: quote[a''b]");
	写格式("5. 反斜杠加字符取编码值 \A = %d\r\n",\A);
	写格式("6. 0#中国 取两个汉字的编码值 = %d\r\n",0#中国);
	写格式("7. 0$abcd 取四个英文的编码值 = %d\r\n",0$abcd);
	写格式("8. 十六进制 $0D0A = %d, 0x0D0A = %d\r\n",$0D0A,0x0D0A);
	整数 数=$0D0A & 255;
	写格式("9. $0D0A & 255 取低字节 = %d\r\n",数);
	' 单引号后面一整行是注释
	// 双斜杠后面一整行是注释
	/* 斜杠星号开始
	   到 星号斜杠 结束，可跨行 */
	写文字("程序结束");
结束
