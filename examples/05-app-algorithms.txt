程序类型=2
模块文件=CMD.inc
整数 数据(20);
整数 个数=0;
程序 初始启动
整数 i,j,临时;
循环于(i=0;i<20;i++){数据(i)=(i*37+11) 余 100;个数++;}
写文字("排序前: ");
打印数据;
循环于(i=0;i<个数-1;i++)
{
循环于(j=0;j<个数-1-i;j++)
{
如果(数据(j)>数据(j+1)){临时=数据(j);数据(j)=数据(j+1);数据(j+1)=临时;}
}
}
写文字("冒泡排序后: ");
打印数据;
写文字("100以内素数: ");
整数 计数=0;
循环于(i=2;i<=100;i++)
{
整数 是素=1;
循环于(j=2;j<i;j++){如果(i 余 j=0){是素=0;跳出;}}
如果(是素=1){写格式("%d ",i);计数++;}
}
写字符(13);写字符(10);
写格式("素数个数=%d\r\n",计数);
整数 a=0,b=1,下;
写文字("斐波那契前20项: ");
循环于(i=0;i<20;i++){写格式("%d ",a);下=a+b;a=b;b=下;}
写字符(13);写字符(10);
结束
程序 打印数据
整数 k=0;
循环于(k=0;k<个数;k++){写格式("%d ",数据(k));}
写字符(13);写字符(10);
结束