程序类型=2
模块文件=CMD.inc

程序 初始启动
	写文字("== bas-5 如果/否则 与 判断 =");
	整数 分=75;
	如果(分>=60){写文字("及格");}否则{写文字("不及格");}
	如果(分>=90) 写文字("优");
	否则 如果(分>=80) 写文字("良");
	否则 写文字("中（单句可省略大括号）");
	写文字("判断(i) 的多种条件写法：");
	整数 i;
	循环于(i=0;i<10;i++){
		写格式("i=%d -> ",i);
		判断(i)为 0 写文字("零")
		为 1,2 写文字("一二（逗号多值）")
		为 3~5 写文字("三到五（波浪号区间）")
		为 <8 写文字("小于八（比较条件）")
		否则 写文字("其它")
	}
	写文字("程序结束");
结束
