模块文件=GUI.inc
整数 窗体;
文本 甲[512];
文本 乙[512];
文本 提示[512];
程序 加载窗体
	窗体=创建窗口($100,程序.名称,"mt",$10C80000,100,100,520,320,0,0,0,0);
	创建窗口($0,"Static","A1",$50000000,10,10,500,24,窗体,1,2,0);
	创建窗口($0,"Static","A2",$50000000,10,40,500,24,窗体,2,2,0);
	创建窗口($0,"Static","A3",$50000000,10,70,500,24,窗体,3,2,0);
	创建窗口($0,"Static","A4",$50000000,10,100,500,24,窗体,4,2,0);
	整数 n=模块名称(0,甲,512);
	改控件字(窗体,1,甲);
	格式化(提示,"模块名称 n=%d",n);
	改控件字(窗体,2,提示);
	整数 m=当前目录(512,乙);
	改控件字(窗体,3,乙);
	格式化(提示,"当前目录 m=%d",m);
	改控件字(窗体,4,提示);
	读取消息
结束
程序 窗体消息(窗口,消息,参数,数据)
	判断(消息)
	为 窗口关闭{停止(0)}
	否则{处理窗口(窗口,消息,参数,数据)}
结束