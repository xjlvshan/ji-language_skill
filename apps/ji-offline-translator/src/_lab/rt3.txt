模块文件=GUI.inc
整数 窗体,编辑,钮;
文本 乙[256];
文本 提示[512];
文本 输入文字[256];

程序 加载窗体
	窗体=创建窗口($100,程序.名称,"rt3",$10C80000,100,100,700,400,0,0,0,0);
	编辑=创建窗口($200,"Edit","ABC-测试文本",$50211004,10,10,660,60,窗体,1,4,0);
	钮=创建窗口($0,"Button","读",$50000000,10,80,100,30,窗体,3,3,0);
	创建窗口($0,"Static","R1",$50000000,10,120,660,24,窗体,2,2,0);
	读取消息
结束

程序 窗体消息(窗口,消息,参数,数据)
	判断(消息)
	为 窗口关闭{停止(0)}
	为 单击左键{命令(参数);返回(0)}
	否则{处理窗口(窗口,消息,参数,数据)}
结束

程序 命令(参数)
	整数 编号;
	编号=参数 & 65535;
	如果(编号=3){试读;返回(0)}
结束

程序 试读
	整数 长;
	乙 ~ 编辑;
	长=文字长度(乙);
	格式化(提示,"嵌套 读入=%d 值=[%s]",长,乙);
	改控件字(窗体,2,提示);
	输入文字 ~ 编辑;
	长=文字长度(输入文字);
	格式化(提示,"嵌套2 读入=%d 值=[%s]",长,输入文字);
	改控件字(窗体,2,提示);
结束
