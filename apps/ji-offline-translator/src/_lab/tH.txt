程序类型=2
模块文件=CMD.inc
文本 源[64];
程序 初始启动
整数 p=0,c=0,n=0;
放置(源,"hello,world");
循环(1)
{
c=源(p);
如果(c=32){p++}
否则
{
如果(c=9){p++}
否则
{
如果(c=13){p++}
否则
{
如果(c=10){p++}
否则{跳出}
}
}
}
写格式("H ok p=%d",p);
结束
程序 放置(目标,字)
整数 n=文字长度(字);
内存复制(目标,字,n);
目标(n)=0;
结束