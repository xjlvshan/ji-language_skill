模块文件=GUI.inc
整数 窗体,编辑;
文本 甲[256];
文本 乙[256];
文本 提示[256];

程序 加载窗体
	整数 n;
	窗体=创建窗口($100,程序.名称,"rt",$10C80000,100,100,700,400,0,0,0,0);
	编辑=创建窗口($200,"Edit","ABC-测试文本",$50211004,10,10,660,60,窗体,1,4,0);
	创建窗口($0,"Static","R1",$50000000,10,90,660,24,窗体,2,2,0);
	创建窗口($0,"Static","R2",$50000000,10,120,660,24,窗体,3,2,0);
	创建窗口($0,"Static","R3",$50000000,10,150,660,24,窗体,4,2,0);
	n=取控件字(窗体,1,甲,256);
	格式化(提示,"取控件字(4参) n=%d 值=[%s]",n,甲);
	改控件字(窗体,2,提示);
	发送消息(编辑,$000D,256,乙);
	格式化(提示,"发送消息 WM_GETTEXT 值=[%s]",乙);
	改控件字(窗体,3,提示);
	格式化(提示,"编辑句柄=%d",编辑);
	改控件字(窗体,4,提示);
	读取消息
结束

程序 窗体消息(窗口,消息,参数,数据)
	判断(消息)
	为 窗口关闭{停止(0)}
	否则{处理窗口(窗口,消息,参数,数据)}
结束
