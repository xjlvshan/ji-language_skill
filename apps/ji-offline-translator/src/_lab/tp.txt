模块文件=GUI.inc
整数 窗体;
文本 全局[512];
程序 加载窗体
	窗体=创建窗口($100,程序.名称,"tp",$10C80000,100,100,600,400,0,0,0,0);
	创建窗口($0,"Static","T1",$50000000,10,10,560,24,窗体,1,2,0);
	创建窗口($0,"Static","T2",$50000000,10,40,560,24,窗体,2,2,0);
	创建窗口($0,"Static","T3",$50000000,10,70,560,24,窗体,3,2,0);
	创建窗口($0,"Static","T4",$50000000,10,100,560,24,窗体,4,2,0);
	创建窗口($0,"Static","T5",$50000000,10,130,560,24,窗体,5,2,0);
	改控件字(窗体,1,"字面量直接设置");
	放置(全局,"放置写入的文字");
	改控件字(窗体,2,全局);
	格式化(全局,"格式化 %d %s",123,"尾部");
	改控件字(窗体,3,全局);
	整数 n=模块名称(0,全局,512);
	改控件字(窗体,4,全局);
	格式化(全局,"模块名称返回 %d",n);
	改控件字(窗体,5,全局);
	读取消息
结束
程序 窗体消息(窗口,消息,参数,数据)
	判断(消息)
	为 窗口关闭{停止(0)}
	否则{处理窗口(窗口,消息,参数,数据)}
结束
程序 放置(目标,字)
	整数 n=文字长度(字);
	内存复制(目标,字,n);
	目标(n)=0;
结束