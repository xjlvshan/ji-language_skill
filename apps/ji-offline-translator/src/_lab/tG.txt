程序类型=2
模块文件=CMD.inc
文本 报文[65536];
整数 报文位=0;
程序 初始启动
报重置;
报文字("abc");
报引号;
报文字("def");
写格式("G ok %s 位=%d",报文,报文位);
结束
程序 报重置
报文位=0;
报文(0)=0;
结束
程序 报文字(内容)
整数 n=文字长度(内容);
内存复制(报文+报文位,内容,n);
报文位=报文位+n;
报文(报文位)=0;
结束
程序 报引号
报文(报文位)=34;
报文位=报文位+1;
报文(报文位)=0;
结束