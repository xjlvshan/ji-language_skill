程序类型=2
模块文件=CMD.inc
文本 出口[256];
文本 源[512];
程序 初始启动
读入(源,"D:\SEC\project\ji-offline-translator\src\sample.json");
写格式("源=[%s]",源);
写字符(13);写字符(10);
整数 n=取键文字(源,"result",出口,256);
写格式("u6a n=%d 值=[%s]",n,出口);
写字符(13);写字符(10);
n=取键文字(源,"n",出口,256);
写格式("u6b n=%d 值=[%s]",n,出口);
写字符(13);写字符(10);
n=取键文字(源,"err",出口,256);
写格式("u6c n=%d 值=[%s]",n,出口);
写字符(13);写字符(10);
n=取键文字(源,"missing",出口,256);
写格式("u6d n=%d 值=[%s]",n,出口);
结束
程序 读入(缓冲,路径)
整数 句柄=文件打开(路径,"rb");
如果(句柄=0){缓冲(0)=0;返回(-1)}
整数 长度=文件大小2(句柄);
文件读取(缓冲,1,长度,句柄);
文件关闭(句柄);
缓冲(长度)=0;
返回(长度)
结束
程序 文件大小2(句柄)
定位文件(句柄,0,2);
整数 长=文件位置(句柄);
定位文件(句柄,0,0);
返回(长)
结束
程序 取键文字(源,键名,出口,出口大小)
整数 p=找键(源,键名);
如果(p<0){出口(0)=0;返回(-1)}
p=跳空白(p);
整数 c=0,n=0,完=0;
如果(源(p)=34)
{
p++;
循环
{
c=源(p);
如果(c=0){完=1}
如果(完=0)
{
如果(c=34){完=1;p++}
如果(完=0)
{
如果(n<出口大小-1){出口(n)=c;n++}
p++;
}
}
如果(完=1){跳出}
}
}
否则
{
循环
{
c=源(p);
如果(c=0){完=1}
如果(c=44){完=1}
如果(完=0)
{
如果(n<出口大小-1){出口(n)=c;n++}
p++;
}
如果(完=1){跳出}
}
}
出口(n)=0;
返回(n)
结束
程序 跳空白(p)
整数 c=源(p),走=1;
循环
{
走=0;
如果(c<33)
{
如果(c>0){p++;c=源(p);走=1}
}
如果(走=0){跳出}
}
返回(p)
结束
程序 找键(源,键名)
整数 键长=文字长度(键名),p=0,序号=0,q=0,好=0;
循环(源(p)<>0)
{
好=0;
如果(源(p)=34)
{
序号=0;
循环(序号<键长)
{
如果(源(p+1+序号)<>键名(序号)){序号=键长+99}
否则{序号++}
}
如果(序号=键长){好=1}
}
如果(好=1)
{
如果(源(p+1+键长)=34)
{
q=跳空白(p+2+键长);
如果(源(q)=58){返回(q+1)}
}
}
p++;
}
返回(-1)
结束