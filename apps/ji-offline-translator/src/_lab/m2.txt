程序类型=2
模块文件=CMD.inc
文本 出口[64];
文本 显示[64];
程序 初始启动
放置(显示,"abc");
写文字("m2a");
整数 值=取键文字(显示,"result",出口,64);
写格式("m2b 值=%d",值);
结束
程序 放置(目标,字)
整数 n=文字长度(字);
内存复制(目标,字,n);
目标(n)=0;
结束
程序 取键文字(源,键名,出口,出口大小)
如果(出口大小>0){出口(0)=0}
返回(-1)
结束