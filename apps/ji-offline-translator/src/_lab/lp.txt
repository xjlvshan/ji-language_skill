程序类型=2
模块文件=CMD.inc
程序 初始启动
	整数 次数,端口;
	次数=0;
	端口=0;
	循环(次数<10)
	{
		次数++;
		端口=12345;
		如果(端口>0){次数=9999}
	}
	写格式("A 次数=%d 端口=%d\r\n",次数,端口);
	次数=0;
	端口=0;
	循环(次数<10)
	{
		次数++;
		端口=12345;
		如果(端口>0){跳出}
	}
	写格式("B 次数=%d 端口=%d\r\n",次数,端口);
结束