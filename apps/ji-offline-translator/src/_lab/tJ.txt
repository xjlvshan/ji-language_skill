程序类型=2
模块文件=CMD.inc
文本 源[256];
文本 出口[256];
程序 初始启动
放置(源,"{  " + 引 + "result" + 引 + ": " + 引 + "hello, world" + 引 + ", " + 引 + "n" + 引 + ": 5}");
整数 n=取键文字(源,"result",出口,256);
写格式("J ok n=%d 值=%s",n,出口);
结束
程序 放置(目标,字)
整数 n=文字长度(字);
内存复制(目标,字,n);
目标(n)=0;
结束
程序 引
返回(0)
结束
程序 取键文字(源,键名,出口,出口大小)
整数 p=找键(源,键名);
如果(p<0){出口(0)=0;返回(-1)}
p=跳空白(p);
整数 c=0,n=0;
如果(源(p)<>34){跳到 裸值}
p++;
循环(1)
{
c=源(p);
如果(c=0){跳到 收尾}
如果(c=34){p++;跳到 收尾}
如果(c=92){p++;c=源(p);c=反转义(c)}
如果(n<出口大小-1){出口(n)=c;n++}
p++;
}
裸值:
循环(1)
{
c=源(p);
如果(c=0){跳到 收尾}
如果(c=44){跳到 收尾}
如果(c=125){跳到 收尾}
如果(c=93){跳到 收尾}
如果(n<出口大小-1){出口(n)=c;n++}
p++;
}
收尾:
出口(n)=0;
返回(n)
结束
程序 反转义(c)
如果(c=110){返回(10)}
如果(c=114){返回(13)}
如果(c=116){返回(9)}
返回(c)
结束
程序 跳空白(p)
整数 c=源(p),走=1;
循环(走=1)
{
走=0;
如果(c<33)
{
如果(c>0){p++;c=源(p);走=1}
}
}
返回(p)
结束
程序 找键(源,键名)
整数 键长=文字长度(键名),p=0,序号=0,q=0;
循环(源(p)<>0)
{
如果(源(p)=34){跳到 试探}
跳到 下一个
试探:
序号=0;
循环(序号<键长)
{
如果(源(p+1+序号)<>键名(序号)){跳到 下一个}
序号++;
}
如果(源(p+1+键长)=34)
{
q=跳空白(p+2+键长);
如果(源(q)=58){返回(q+1)}
}
下一个:
p++;
}
返回(-1)
结束