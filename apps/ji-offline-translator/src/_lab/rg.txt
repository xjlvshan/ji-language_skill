模块文件=GUI.inc
整数 窗体;
文本 根目录[512];
文本 路径甲[512];
文本 路径乙[512];
文本 日志路径[512];
文本 日志内容[8];
整数 日志句柄;
程序 加载窗体
	放置(日志路径,"D:\SEC\project\ji-offline-translator\debug.log");
	写标记("1");
	整数 n=模块名称(0,根目录,512);
	写标记("2");
	整数 i=n-1;
	循环(i>0)
	{
		如果(根目录(i)=92){根目录(i)=0;i=0}
		否则{i--}
	}
	写标记("3");
	拼路径(路径甲,根目录,"engine\python.exe");
	写标记("4");
	整数 有=有文件(路径甲);
	写标记("5");
	拼路径(路径甲,根目录,"..\engine\python.exe");
	拼路径(路径乙,根目录,"..\bridge.py");
	写标记("6");
	窗体=创建窗口($100,程序.名称,"rg",$10C80000,100,100,500,300,0,0,0,0);
	改控件字(窗体,1,根目录);
	读取消息
结束
程序 窗体消息(窗口,消息,参数,数据)
	判断(消息)
	为 窗口关闭{停止(0)}
	否则{处理窗口(窗口,消息,参数,数据)}
结束
程序 写标记(码)
	放置(日志内容,码);
	日志句柄=文件打开(日志路径,"ab");
	如果(日志句柄>0){文件写入(日志内容,1,1,日志句柄);文件关闭(日志句柄)}
结束
程序 拼路径(基底,前缀,尾部)
	放置(基底,前缀);
	整数 n=文字长度(基底);
	内存复制(基底+n,尾部,文字长度(尾部));
	基底(n+文字长度(尾部))=0;
结束
程序 有文件(路径)
	整数 句柄=文件打开(路径,"rb");
	如果(句柄=0){返回(0)}
	文件关闭(句柄);
	返回(1)
结束
程序 放置(目标,字)
	整数 n=文字长度(字);
	内存复制(目标,字,n);
	目标(n)=0;
结束